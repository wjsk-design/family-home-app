# わが家ホーム — 開発引き継ぎドキュメント

家庭向けLINEミニアプリ(家族の日常を管理する「家庭のホーム画面」)。
**エキテンのLINEミニアプリ化PoCの前段検証**という位置づけの個人開発プロジェクト。

## 依頼主について(重要)

- 非エンジニア。GASは自走できる。AI支援前提で開発している
- 報告・説明はすべて日本語で。専門用語は噛み砕く
- 育児中(妻・子2人)。このアプリの最初の実ユーザーは家族
- 金融機能(口座連携・決済・家計簿API等)は**仕様として対象外**。追加しないこと

## 実行方法(ビルド不要)

- この端末には **Node.jsが無い**。npm/Vite/React/Tailwindは使えない。導入提案もしない
- `index.html` をブラウザで開くだけで動く(ES Modules不使用なので file:// でも動作)
- ローカルサーバーで確認する場合: `py -m http.server 8642`(python可・nodeなし)
- データは localStorage(キー: `wagaya-home-v1`)。リセットはメニュー画面の「データを初期化」か `localStorage.clear()`

### ⚠️ キャッシュ対策(ハマりポイント)

ブラウザがJS/CSSを強くキャッシュする。**js/cssを変更したら必ず `index.html` の全参照のバージョンクエリ(`?v=0.2.0`)を上げること**(全19箇所、一括置換でよい)。これを忘れると変更が反映されず延々ハマる(実績あり)。

## アーキテクチャ

Vanilla JS SPA。フレームワークなし。`window.App` 名前空間にすべてぶら下がる。

```
index.html          エントリ。scriptの読み込み順に意味がある(下記の順を守る)
css/tokens.css      デザイントークン(色・余白・角丸・影・文字・z-index・モーション)
css/base.css        リセット+骨格(max-width 480px中央寄せ・セーフエリア・100dvh)
css/components.css  共通部品(card/sheet/toast/nav/fab/chip/task-item/form…)
css/screens.css     画面固有スタイル
js/config.js        APP_CONFIG。LIFF_IDはここ(空=モックモード)
js/icons.js         App.icon(name, size) — アウトラインSVG。絵文字をUIアイコンにしない
js/holidays.js      App.holidayName(dateStr) — 日本の祝日判定(振替休日・国民の休日込み、算出式)
js/store.js         App.store(load/save/update/reset)+モックシード+App.data参照ヘルパー
js/ui.js            App.el(DOM生成)/sheet/toast/confirm/field/chipSelect/taskItem/
                    sectionHeader/emptyState/fab/memberBadges/openStatusSheet
js/liff.js          App.initLiff — LIFF_ID未設定ならモック、設定済ならSDK動的ロード
js/screens/*.js     各画面。App.screens.<route> = { title, nav|back, render(container) }
js/app.js           ハッシュルーティング(#home等)・ヘッダー・下部ナビ・App.go/App.refresh
```

### 画面の追加・変更のルール

1. 画面は `App.screens.<route>` に登録するだけでルーティングされる
   - `nav: "home"` → 下部ナビのタブとして点灯 / `back: true` → 戻るボタン付きサブ画面
2. データ変更は必ず `App.store.update((st) => {...})` 経由。保存と再描画(スクロール位置維持)が自動で走る
3. AI相談画面のみ例外的に `App.store.save()`+手動DOM追加(全再描画するとチャットが跳ねるため)
4. 古いlocalStorageデータに新フィールドが無いケースを必ずガードする(例: `p.careTasks || []`)。シード変更は既存ユーザーには反映されない
5. 削除系は必ず `App.confirm()`(シート型確認)を挟む。操作後は必ず `App.toast()`

## デザイン原則(厳守)

コンセプト: Apple Homeのカード × 無印の余白 × 北欧の温かい色 × Things 3の操作感。
「高機能に見える」より「毎日使っても疲れない」。

- **色は必ずtokens.cssのCSS Variablesから**。ハードコード禁止
  - Background `#F8F7F3` / Primary `#5E7B71` / Accent `#D6B27A` / 文字 `#2B2B2B`(真っ黒禁止)
  - カテゴリ色(family/calendar/task/shopping/plant/note/ai)は**アイコン・ラベル・薄背景のみ**。カード全体を塗らない。1画面で強いアクセントを競合させない
- 余白8px基準 / 角丸16px(大カード20px) / 影は `--shadow-card` 1種のみ(極薄)
- 中央モーダル禁止 → **Bottom Sheet**(`App.sheet`)を使う
- 空状態は全リストに文言付きで用意(「今日は予定なし。ゆっくり過ごせそうです」調)
- エラー文言は技術用語を出さない平易な日本語
- タップ領域44px以上 / aria-label / フォーカスリング維持 / `prefers-reduced-motion` 対応
- LINEグリーン多用禁止・強グラデ禁止・ダッシュボード的な数字詰め込み禁止

## 現在の状態(v0.10.0・全機能ブラウザ検証済み)

10画面: ホーム / カレンダー / やること / 買い物 / 家族のようす / 植物 / メモ日記 / AI相談(デモ応答) / メニュー / データ管理

直近の変更:
- **予定に色を付けられるように**(2026-07-14・v0.10.0): メンバー識別色と同じ北欧トーン6色+標準(緑)の計7色から選択。`App.paletteColor(n)` / `App.colorSwatches(selected, onChange, {includeStandard})`(`js/ui.js`、家族の色スウォッチも共通化してこちらに統合)を追加。event.color(0〜6、未設定は標準)としてカレンダーのマス内チップ・選択日一覧・ホームの今日の予定、それぞれに反映(一覧系は行全体でなく小さな色ドットのみ、マス内チップは背景ごと着色)
- **くり返し予定(毎週◯曜日/毎月◯日)を追加**(v0.10.0): 「ルールを保存して都度計算」ではなく**先の期間分の実体イベントを一括作成**する方式(`recurDates()`、`js/screens/calendar.js`)。同じ回は`seriesId`で束ねる。新規追加時のみ選択可(編集は常に単発扱い)。「いつまで」既定は開始日の1年後。毎月31日など存在しない月はその回だけスキップ。削除時、`seriesId`がある予定は「この日だけ削除/くり返しをすべて削除」の選択シート(`confirmDeleteSeries`)を出す。**タイトル等の一括変更(全回まとめて編集)は未対応**、必要になれば次段階で検討

直近の変更:
- **カレンダーのマス内切り詰めで、装飾付き絵文字が文字を食い尽くす不具合を修正**(2026-07-14・v0.9.5): `clipChars`(`js/screens/calendar.js`)が`[...text]`(コードポイント単位)で数えていたため、性別記号付きの絵文字(例:🏊‍♀️ = 人+ZWJ+性別記号+異体字セレクタの4コードポイント)が4文字予算を単独で使い切り、後続の文字が消えていた(例:「🏊‍♀️ こたプール」→絵文字のみ表示)。`Intl.Segmenter({granularity:"grapheme"})`で「人の目に1文字に見える単位」ごとに数えるよう修正(Intl.Segmenter非対応環境向けに`[...text]`へのフォールバックも保持)。単純な絵文字(⚽等、1コードポイント)は元々問題なかった

直近の変更:
- **LINE内ブラウザ自体のヘッダーで上部が占有される分、カレンダーの上部をさらに圧縮**(2026-07-14・v0.9.4): `noHeader`画面(今はカレンダーのみ)で`#app-header`に`app-header--compact`クラスを付与(`js/app.js`)し、`padding-top`をセーフエリア+16px→+8pxに(`css/base.css`)。月ナビ(`.cal-nav`)自体も`min-height:52px`指定を削除(アイコンボタンの44pxが自然な高さになる)+`margin-bottom`12px→8px(`css/screens.css`)。**他画面のヘッダーには影響しない**(`app-header--compact`は`noHeader`画面にのみ付与)

直近の変更:
- **カレンダーのマス内予定を3件表示・「+n件」廃止**(2026-07-14・v0.9.3): 下部の「選択日の予定」パネルで全件確認できるため、マス内は最大3件表示して切り捨て(`.cal-day__more`は未使用化に伴い削除)。`.cal-day` の `min-height` を72pxに拡大して3件分を収容
- **下部「選択日の予定」パネルの余白を圧縮**(v0.9.3): 上マージン24px→16px(`.cal-day-section`)、カード内の上下パディング20px→12px(`.cal-day-card`、左右20pxは維持)。これらは専用クラスでの上書きなので他画面の`.section`/`.card--lg`には影響しない

直近の変更:
- **カレンダー画面の共通ヘッダーを廃止し、月ナビを見出しに**(2026-07-14・v0.9.2): 下部ナビの「カレンダー」タブと文言が重複していたため。`App.screens.calendar.noHeader = true` を追加し、`app.js`の`renderHeader`がこのフラグを見て共通ヘッダー(タイトルバー)を出さないようにした(`#app-header`のセーフエリア分paddingだけは残るのでノッチには被らない)。`.cal-nav__label`をページ見出しサイズ(`--text-page-title`)に拡大して「2026年7月」自体が見出しとして機能する。**他画面で見出しを消したい場合はこのパターン(`noHeader: true`)を使う**
- **カレンダーのマス内予定名を「…」省略ではなく4文字固定に**(v0.9.2): 「…」自体が幅を取り実質2〜3文字しか見えていなかったため、`clipChars(text, 4)`(`js/screens/calendar.js`、スプレッド演算子で絵文字のサロゲートペアも壊さず処理)でJS側から4文字ちょうどに切り、CSSの`text-overflow: ellipsis`は廃止(`overflow: hidden`のみ保険で残す)
- **日付タップ後に画面が右にずれる不具合を修正**(v0.9.2): 3つの原因に対処。①`App.refresh()`(`js/app.js`)が縦スクロール位置しか復元していなかった→横位置(`scrollX`)も保存・復元。②input/textarea/selectのフォントが15pxで、iOS Safari/LINE内ブラウザの「16px未満は自動ズーム」仕様に触れていた→`base.css`で16pxに統一。③保険として`html, body { overflow-x: hidden; }` を追加し、今後同種の要素はみ出しが起きても横スクロール自体が発生しないようにした

直近の変更:
- **カレンダーをYahoo!カレンダー風「上カレンダー常時表示+下に選択日詳細」に戻す**(2026-07-14・v0.9.1): v0.9.0で追加した「日付タップ→シート表示」(`openDaySheet`)は**廃止**し、日付タップは`view.selected`更新のみに戻した。代わりにグリッド下へ「選択日の予定」パネルを常時表示で再設置(月の上旬でも下部が隠れないよう、下記のコンパクト化とセット)
  - **カレンダー本体をコンパクト化**: `.cal-day` の `min-height` 78px→52px、マス内の予定チップは最大**2件**+「+n件」(3件から変更)。`.cal-grid`の余白を`card--lg`の20pxから8px相当に詰め、マス間の隙間も2px→1pxに圧縮
  - **列幅を拡大して4文字表示に**: 上記の余白圧縮で列幅が約55px→約59pxに拡大。チップのフォントも10px→11pxに上げ、全角4文字が安定して収まるようになった(canvas計測で確認済み)
  - 「日付タップでシート」自体のコンセプト(=タップ時に詳細が確実に見える)は、この常時表示パネル+コンパクト化で別の形で実現している。**将来また「シート派」に戻すことがあれば、このコミット付近の差分を参照**

直近の変更:
- **カレンダー: 日付タップでその日の予定をシート表示**(2026-07-14・v0.9.0): 月上旬など画面下部までスクロールしないと予定が見えなかった問題を解消。`openDaySheet(dateStr)`(`js/screens/calendar.js`)が日付ごとの予定一覧をBottom Sheetで表示(祝日名付きタイトル・予定タップで編集・空状態・「この日に予定を追加」)。旧来の画面下部「選択日の予定」セクションは重複のため廃止。グリッドの日付ボタンは選択状態の更新+シートを開く両方を行う
- **シートの自動キーボード表示を廃止(全画面共通)**(v0.9.0): `App.sheet`(`js/ui.js`)が開いた瞬間に最初のinput/textareaへ`.focus()`していたため、iPhoneでキーボードが即座に開き画面がその入力欄へ強制スクロールされ「シートのタイトルが見えないまま入力欄にいる」状態になっていた(ユーザー報告の動画で確認)。修正後はシート自体(`tabindex="-1"`)にフォーカスし、キーボードは出さない。ユーザーが欄をタップして初めて開く通常の挙動に。**バリデーション時(未入力で保存を押した場合)の該当欄への`focus()`は意図的に残しており、影響なし**。この修正は共通部品のため全ての追加/編集シート(予定・やること・メモ・家族・植物・チーム名等)に一括で効く

直近の修正:
- **カレンダーの列崩れを修正**(2026-07-14・v0.8.1): 長い予定名(改行不可のテキスト)がマスを内側から押し広げ、右側の列(金・土・日)が画面外に押し出される不具合。原因はCSS Grid/Flexboxの既定`min-width: auto`で、子要素の内容がコンテナの最小幅を決めてしまうこと。`.cal-grid`の`grid-template-columns`を`repeat(7, 1fr)`→`repeat(7, minmax(0, 1fr))`に、`.cal-day`に`min-width: 0; overflow: hidden;`、`.cal-day__chip`に`min-width: 0;`を追加して解決。**同種の「長いテキストで枠が崩れる」不具合が今後別画面で起きたら、まずこの`min-width: 0`忘れを疑うこと**

直近で追加した機能:
- **カレンダーをYahoo!カレンダー風に刷新**(2026-07-14・v0.8.0):
  - **月曜始まり**(月火水木金土日の並び)。`js/screens/calendar.js` の週開始計算を月曜基準に変更(`offsetToMonday = (first.getDay()+6)%7`)
  - **マスに予定名を直接表示**。1日最大3件まで予定名をそのまま表示、4件以上は3枠目が「+n件」に切り替わる(`MAX_CHIPS = 3`)。マスは正方形→縦長(`min-height: 78px`)に変更、CSS `.cal-day__chip`/`.cal-day__more`
  - **週数が可変**(その月に必要な5週=35マス or 6週=42マスのみ描画。以前は常に42マス固定)
  - **土曜=青系(`--cal-sat`)・日曜/祝日=赤系(`--cal-sun`)**で日付数字とヘッダー文字だけ色分け(マス全体は塗らない)。祝日は平日でも赤扱い
  - **祝日判定は`js/holidays.js`で算出式により計算**(固定日+ハッピーマンデー+春分秋分の近似式+振替休日+国民の休日)。静的な年ごとのリストではなく計算式なので**メンテ不要**(2000〜2099年の範囲で有効な式)。`App.holidayName(dateStr)` で取得。選択日の見出し・aria-labelにも祝日名を表示
  - 選択中の日は「塗りつぶし背景」→「枠線+薄背景」に変更(予定チップと競合しないように)。今日の日付は丸バッジで強調
  - 予定チップの色は統一(`--cat-calendar-bg`系)。予定ごとの色分けはアプリの原則(強いアクセントを競合させない)により不採用
- **柏レイソル2026シーズン日程を一括登録**(2026-07-14・v0.7.1): `KASHIWA_2026_FIXTURES`(`js/screens/menu.js`)にJ.League公式サイト(jleague.jp、2026-07-14時点)から取得した実際の日程21試合(J1リーグ20+ルヴァンカップ1。天皇杯は対戦相手未定のため除外)をハードコード。応援チームが「柏レイソル」の時だけ、サンプル日程ボタンが「柏レイソルの2026シーズン日程を登録する」に切り替わり(`syncImportLabel`、チーム名入力に連動)、押すと一括登録される。日程未確定の試合(AFCクラブ競技会の抽選待ち、8/18確定予定)は`time:""`(終日)+`memo`に節番号と未確定の理由を記載。**あくまで現時点のスナップショットを手動転記したもので自動更新はされない**。正式には優先順位5番のGAS連携で自動化する
- **試合を実際の日程で追加・編集できる汎用UI**(v0.7.0): `openMatchSheet(team, match)`(`js/screens/menu.js`)。対戦相手・日付・キックオフ時間(`App.timeField`再利用)・ホーム/アウェイを入力してカレンダーに`kind:"match"`イベントとして登録。既存の「サンプル日程を取り込む」ボタンは維持(柏レイソル以外のチームや動作確認用)。応援チームシート内に「登録済みの試合」一覧も表示、タップで編集可能
- **データ初期化を「データ管理」画面の奥へ**(2026-07-13・v0.6.0): 滅多に使わず取り返しがつかないため、メニュー直下の1タップから3段構えに変更。メニュー→データ管理(`App.screens.dataManage`、**menu.js内に定義**)→保存データの件数一覧→確認チェック(`.confirm-check`、入れないとボタン無効)→最終確認シート→初期化してホームへ
- **メンバーの色を選べるように**(v0.6.0-0.6.1): 家族編集シートに「この人の色」スウォッチ(6色、`.color-swatch`)。`family[].color`(1〜6)に保存し、`App.memberColor` が明示色を優先(未設定は従来どおり並び順で自動)。他メンバーと重複する色を選ぶと「〇〇と同じ色です(そのままでも大丈夫)」とヒント表示(ブロックはしない)
- **管理者権限は未実装(将来課題)**: 「初期化・家族削除・メンバー編集を管理者だけに表示したい」という要望あり。現状はアカウントの仕組みが無い(localStorage・単一端末)ため実装不可。**LIFFログイン導入時に、LINE userIdベースの管理者判定で破壊的操作をゲートする**設計とすること
- **データ初期化を「データ管理」画面の奥へ**(2026-07-13・v0.6.0): 滅多に使わず取り返しがつかないため、メニュー直下の1タップから3段構えに変更。メニュー→データ管理(`App.screens.dataManage`、**menu.js内に定義**)→保存データの件数一覧→確認チェック(`.confirm-check`、入れないとボタン無効)→最終確認シート→初期化してホームへ
- **メンバーの色を選べるように**(v0.6.0-0.6.1): 家族編集シートに「この人の色」スウォッチ(6色、`.color-swatch`)。`family[].color`(1〜6)に保存し、`App.memberColor` が明示色を優先(未設定は従来どおり並び順で自動)。他メンバーと重複する色を選ぶと「〇〇と同じ色です(そのままでも大丈夫)」とヒント表示(ブロックはしない)
- **管理者権限は未実装(将来課題)**: 「初期化・家族削除・メンバー編集を管理者だけに表示したい」という要望あり。現状はアカウントの仕組みが無い(localStorage・単一端末)ため実装不可。**LIFFログイン導入時に、LINE userIdベースの管理者判定で破壊的操作をゲートする**設計とすること
- **時間ピッカー:時だけ選んでも終日扱いにしない**(2026-07-13・v0.5.1): 時をタップした時点で分が未選択なら「00」を自動選択(視覚的にもハイライト)。分だけ選んで決定した場合は閉じずに「何時にするかも選んでください」とトースト案内
- **絵文字アバターを全廃し頭文字アバターに統一**(v0.5.1): `App.initialAvatar(name, memberId)`(`js/ui.js`)を追加し、ホーム家族カード・家族画面・メニューのプロフィール・ホーム右上ボタンをすべて頭文字+メンバー色に統一。自分(settings.userName)は同名の家族がいればその色を継承。家族編集シートの絵文字ピッカーは廃止(アイコンは名前から自動)。`family[].avatar` / `settings.userAvatar` フィールドはもう読まれない(古いデータに残っていても無害)
- **ホームの予定タップで詳細シート**(2026-07-13・v0.5.0): ホーム「今日の予定」の行をボタン化し、タップで詳細シート(`openEventDetailSheet`、`js/screens/home.js`)を表示。日時・だれの予定・メモ(あれば)+「この予定を編集」ボタン。編集は `App.openEventSheet`(calendar.jsから公開、App.openTaskSheetと同じパターン)を呼ぶ。メモ付き予定はタイトル横に小さなnoteアイコン(`schedule-item__note-icon`)を表示
- **時間ピッカー「終日にする」は即確定で閉じる**(v0.5.0): 押した瞬間に `onPick("")` してシートを閉じる。以前は選択解除だけで、すでに終日の予定だと何も反応がなかった
- **メンバーバッジを絵文字→頭文字+色分けに**(v0.5.0): 小さい絵文字アバターは色味が似て判別しづらいとの指摘で、`App.memberBadges`(`js/ui.js`)を名前の頭文字1文字+メンバー別色のバッジに変更。色は `tokens.css` の `--member-1〜6`(家族の並び順に割り当て、7人目以降は循環)。`App.memberColor(memberId)` で取得可。家族のようすカード等の大きいアバターは絵文字のまま
- **予定にメモを追加できるように**(2026-07-13・v0.4.0): 予定編集シート(`js/screens/calendar.js` `openEventSheet`)に`memo`(任意文字列)を追加。「毎回必要なわけではない」との要望どおり、常時表示ではなく**未入力時は「＋ メモを追加」の1行だけ**を出し、押すとテキストエリアが開く(既に内容がある予定を編集する時は最初から開いた状態)。カレンダー日別一覧ではタイトル下にメモを控えめな薄字1行で表示(`schedule-item__memo`、はみ出しは省略記号)。**ホーム画面の「今日の予定」は簡潔さを優先してメモを表示しない設計判断**(必要ならカレンダー画面で確認する動線)。既存イベント(memoフィールド無し)は`ev.memo`がundefined→falsy扱いで安全に動作
- **時間選択をアプリ内Bottom Sheet型に置き換え**(2026-07-13・v0.3.4): `<input type="time">` はブラウザネイティブのドロップダウンが出てデザインが浮く(CSSで制御不可)との指摘を受けて廃止。`App.timeField(labelText, value, onChange)`(`js/ui.js`)がトリガーボタン+`App.sheet`型の時・分ピッカーを提供。時/分は独立スクロール列(5分刻み)。未設定時の文言は予定一覧の表示(`ev.time || "終日"`)に揃えて**「終日」**で統一(トリガーの初期表示も、クリアボタン「終日にする」も)。カレンダーの予定追加/編集(`js/screens/calendar.js`)で使用。スタイルは `css/components.css` の `.time-trigger` / `.time-picker*`。他画面で時間入力が必要になったら同じ部品を再利用する
- **カレンダー「だれの予定?」の「みんな」チップを別格に**(2026-07-13・v0.3.1): 上記の解決済み論点を参照
- **植物のお世話をやることに自動合流**(2026-07-13・v0.3.0): 水やり期限(残0日以下)とお手入れ適期(期間開始〜/予定日到来)を `App.data.plantCareItems()` が導出し、ホームの「今日やること」先頭とやること画面(両タブ)に表示。**tasksには保存しない導出データ**(idは `plant-water-*` / `plant-care-*`、`kind` フィールドで判別)。チェックすると `App.completePlantCareItem()` が水やり記録 or お手入れ完了(careLogへ)を実行し、条件から外れて自然に消える。編集ボタンは付けない(実体は植物画面で編集)
- カレンダー「だれの予定?」: **新規はデフォルト全員選択**+「みんな」チップ(全選択⇔解除、個別解除と連動)。全員該当は顔アイコンでなく「みんな」バッジ表示(`App.memberBadges`)
- 応援チームの試合予定(メニュー内): チーム名保存+試合の手入力・サンプル日程取り込み(`kind:"match"`)。**柏レイソルのみ実際の2026シーズン日程を一括登録可能**(上記参照)。他チームの自動連携は未対応
- 植物の**お手入れ予定**(`plant.careTasks`): 摘芯/植え替え/肥料/剪定/その他。「この日」`mode:"date"` と「期間」`mode:"range"`(〇日〜〇日)の2方式。期間中は「いま適期」バッジ。完了で `careLog` に履歴

## 未解決のデザイン論点

- ~~カレンダー「だれの予定?」のチップ設計~~ → **解決済み(v0.3.1、2026-07-13)**。「段を分ける」案を採用。`memberSelector()`(`js/screens/calendar.js`)で「みんな(全員)」を独立ボタン(`.member-selector__all`)にし、仕切り線(`.member-selector__divider`)を挟んで個別メンバーチップと分離。スタイルは `css/components.css` の `.member-selector*`

## 公開・連携情報(2026-07-15更新)

- **GitHubリポジトリ**: https://github.com/wjsk-design/family-home-app (Public、claude-work全体とは独立した専用リポジトリ)
  - 2026-07-15、会社アカウント`watanabejunsuke-rgb`から私物アカウント`wjsk-design`へownership transferで移管済み(退職時の消失リスク回避)。旧URLは自動でリダイレクトされる
  - コミットの作成者は個人特定を避けるためGitHub no-replyアドレスを使用(`wjsk-design <305059244+wjsk-design@users.noreply.github.com>`)。ローカルの`git config`もこのリポジトリ限定でこの値に設定済み
- **公開URL(GitHub Pages)**: https://wjsk-design.github.io/family-home-app/
- **LINE Developers**: プロバイダー`junsuke-personal`(仕事用の`eki-salesops`とは別。個人プロジェクトなので分離)→チャネル「わが家ホーム」(LINEログイン種別)
- **LIFF ID**: `2010693415-ddc2Kd3X`(`js/config.js`に設定済み・本番モード)。LIFF URL: `https://liff.line.me/2010693415-ddc2Kd3X`
  - リポジトリ移管に伴い公開URLが変わったため、LIFFのEndpoint URLを新URLに更新する必要あり(LIFF ID・チャネルIDは不変)
- スマホのLINEアプリでLIFF URLを開き、LINE表示名の連携まで動作確認済み(2026-07-14)

## 次にやる作業(優先順)

1. ~~お手入れ適期・水やり期限をホームの「今日やること」に自動合流~~ → **完了(v0.3.0)**
2. ~~GitHub Pages公開 → LIFF ID発行 → config.js設定~~ → **完了(2026-07-14)**。上記「公開・連携情報」参照
3. **GAS+スプレッドシートのバックエンド化**(store.jsのload/saveを差し替え)→ 家族間同期。ユーザーはGASが得意なのでこの構成が最適
4. **Messaging APIプッシュ通知**(予定リマインド・水やり・試合前日)— 「届くアプリ」化がPoCの本丸
5. Jリーグ公式日程との実連携(GASを中継サーバーにしてCORS回避。menu.jsのopenTeamSheetにサンプル実装あり)
6. AI相談の実API化(ai.jsの `mockReply` を差し替え)

## 関連資料

- **設計メモ(実装保留)**: `docs/plan-ai-consult-history.md` — AI植物相談履歴連携(ChatGPT→GPT Actions→GAS→ミニアプリ)の将来設計(2026-07-13)。**着手前に必読**。特に「plantId安定化が前提条件」「相談履歴は独立コレクション」「構造化項目はGPTに埋めさせる」の3点

- 企画書Artifact「わが家の行きつけ帖 MVP仕様書」(https://claude.ai/code/artifact/99132a8f-8a22-4082-bd32-b8222f4d4ecb) — 当初の別コンセプト。エキテンPoCの文脈理解に有用だが、実装されたのは本アプリ(家庭ホーム画面路線)
- 元の実装ブリーフの合格基準: 「開いた瞬間に今日の状況が分かる」「迷わず操作できる」「毎日見ても疲れない」「温かいが幼すぎない」
